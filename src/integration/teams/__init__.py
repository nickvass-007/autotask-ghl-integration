"""Teams Ops Bot (Spec §11): approval Adaptive Cards + transaction feed.

Stage 1 wires Contacts approvals and the transaction feed. A real Azure Bot
Service app is required (not an incoming webhook) so Approve/Reject buttons can
call back into the integration."""
